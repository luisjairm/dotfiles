version: '3.8'

services:
  db:
    image: postgres:17
    container_name: db_17
    environment:
      POSTGRES_DB: prueba
      POSTGRES_USER: 8451
      POSTGRES_PASSWORD: 8451
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"

volumes:
  pgdata:
